<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client action for the custom banner + KPI cards + list dashboard
         (client feedback: default dashboard view will be list view,
         with a banner and #06284F color scheme) -->
    <record id="action_task_dashboard_client" model="ir.actions.client">
        <field name="name">Task Dashboard</field>
        <field name="tag">employee_task_management.task_dashboard</field>
    </record>
</odoo>
