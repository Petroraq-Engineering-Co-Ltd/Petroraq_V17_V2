<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="cron_expire_order_inquiry" model="ir.cron">
        <field name="name">Expire Order Inquiry Without Quotation</field>
        <field name="model_id" ref="model_order_inq"/>
        <field name="state">code</field>
        <field name="code">model._cron_expire_inquiries_without_quotation()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active">True</field>
    </record>

</odoo>
