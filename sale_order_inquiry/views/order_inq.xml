<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_order_inq_form" model="ir.ui.view">
        <field name="name">view_order_inq_form</field>
        <field name="model">order.inq</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="button_confirm" type="object" string="Submit" class="btn-primary"
                            invisible="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                    <button name="button_cancel" type="object" string="Cancel" class="btn-secondary"
                            invisible="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                    <button name="action_create_quotation" type="object" string="Create Quotation"
                            class="btn-info"
                            invisible="state != 'accept' or sale_order_id or inquiry_type != 'trading'"
                            groups="pr_work_order.custom_group_work_order_user"/>
                    <button name="action_create_quotation" type="object" string="Create Estimation"
                            class="btn-info"
                            invisible="state != 'accept' or estimation_id or inquiry_type != 'construction'"
                            groups="pr_work_order.custom_group_work_order_user"/>
                    <button name="action_accept" type="object" string="Accept" class="btn-success"
                            invisible="state == 'pending' or sale_order_id or state == 'cancel' or state == 'reject' or state == 'expire' or state == 'accept' or state in ('estimation_created', 'quotation_created', 'closed')"
                            groups="pr_work_order.custom_group_work_order_user"/>
                    <button name="action_open_reject_wizard" type="object" string="Reject" class="btn-danger"
                            invisible="state == 'pending' or sale_order_id or state == 'cancel' or state == 'reject' or state == 'expire' or state == 'accept' or state in ('estimation_created', 'quotation_created', 'closed')"
                            groups="pr_work_order.custom_group_work_order_user"/>

                    <button name="action_extend_deadline"
                            string="Extend Deadline"
                            type="object"
                            class="btn-warning"
                            groups="petroraq_sale_workflow.group_sale_approval_manager"
                            invisible="state in ('pending', 'cancel', 'confirm', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>

                    <button name="action_reset_to_draft"
                            string="Reset Draft"
                            type="object"
                            class="btn-secondary"
                            groups="petroraq_sale_workflow.group_sale_approval_manager"
                            invisible="state in ('pending', 'confirm', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>

                    <field name="state" widget="statusbar"
                           statusbar_visible="pending,confirm,accept,estimation_created,quotation_created,closed,cancel,reject,expire"/>


                </header>
                <sheet>
                    <div name="button_box" class="oe_button_box">
                        <button icon="fa-pencil-square-o" type="object" string="View Sales" class="oe_stat_button"
                                name="view_sale_order"
                                invisible="not sale_order_id">
                            <field name="sale_count" widget="statinfo" string="Quotation"/>
                        </button>
                        <button icon="fa-calculator" type="object" string="View Estimation" class="oe_stat_button"
                                name="view_estimation"
                                invisible="not estimation_id">
                            <field name="estimation_count" widget="statinfo" string="Estimation"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Submitted" bg_color="bg-success" invisible="state != 'confirm'"/>
                    <widget name="web_ribbon" title="Pending" bg_color="bg-info" invisible="state != 'pending'"/>
                    <widget name="web_ribbon" title="Accepted" bg_color="bg-info" invisible="state != 'accept'"/>
                    <widget name="web_ribbon" title="Estimation Created" bg_color="bg-info"
                            invisible="state != 'estimation_created'"/>
                    <widget name="web_ribbon" title="Quotation Created" bg_color="bg-info"
                            invisible="state != 'quotation_created'"/>
                    <widget name="web_ribbon" title="Closed" bg_color="bg-success" invisible="state != 'closed'"/>
                    <widget name="web_ribbon" title="Cancelled" bg_color="bg-danger" invisible="state != 'cancel'"/>
                    <widget name="web_ribbon" title="Rejected" bg_color="bg-danger" invisible="state != 'reject'"/>
                    <widget name="web_ribbon" title="Expired" bg_color="bg-danger" invisible="state != 'expire'"/>
                    <widget name="web_ribbon" title="Multi" bg_color="bg-success" invisible="sale_count &lt;= 1"/>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group col="2">
                        <group>
                            <field name="inquiry_type" required="1"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="description" required="1"

                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="partner_id" required="1"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <!--                            <field name="company_id" readonly="1"/>-->
                            <field name="email" readonly="1"/>
                            <field name="user_id"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="date_order" readonly="1"/>
                            <field name="state" invisible="1"/>


                        </group>
                        <group>
                            <field name="contact_person" required="1"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="contact_person_email" required="1"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="contact_person_phone" required="1"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="designation"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="deadline_submission" required="1"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>
                            <field name="required_attachment_ids" widget="many2many_binary" required="1"
                                   readonly="state in ('confirm', 'cancel', 'reject', 'expire', 'accept', 'estimation_created', 'quotation_created', 'closed')"/>

                            <!--                            <field name="sale_order_ids" nolable="1" widget="many2many_tags"  invisible="not multi_order" />-->

                        </group>
                    </group>
                    <separator string="Quotation Status" colspan="4" invisible="not sale_order_id"/>
                    <separator string="Estimation Status" colspan="4" invisible="not estimation_id"/>

                    <group col="2">
                        <group>
                            <field name="quotation_main_id" readonly="1" invisible="not sale_order_id"
                                   options="{'no_create': True}"/>
                            <field name="quotation_state" widget="badge" invisible="not sale_order_id" readonly="1"/>
                            <field name="sale_order_id" nolable="1" invisible="1"/>


                        </group>
                        <group>
                            <field name="quotation_approval_state" widget="badge" invisible="not sale_order_id"
                                   readonly="1"/>
                            <field name="quotation_amount_total" invisible="not sale_order_id" readonly="1"/>


                            <field name="sale_order_id" nolable="1" invisible="1"/>

                        </group>
                    </group>
                    <group col="2" invisible="not estimation_id">
                        <group>
                            <field name="estimation_id" readonly="1" options="{'no_create': True}"/>
                        </group>
                        <group>
                            <field name="estimation_id" readonly="1" invisible="1"/>
                        </group>
                    </group>


                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>


    <record id="view_order_inq_tree_view" model="ir.ui.view">
        <field name="name">Order Inquiry Tree View</field>
        <field name="model">order.inq</field>
        <field name="arch" type="xml">
            <tree class="o_sale_order" decoration-muted="state == 'cancel'">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="inquiry_type"/>

                <field name="description"/>
                <field name="partner_id"/>
                <field name="contact_person"/>
                <field name="contact_person_email"/>
                <field name="date_order" optional="show"/>
                <field name="deadline_submission" optional="show"/>
                <field name="user_id" optional="show"/>
                <field name="company_id" optional="show"/>
                <field name="state" widget="badge" decoration-info="state in ('pending', 'estimation_created', 'quotation_created')"
                       decoration-success="state in ('confirm', 'accept', 'closed')"
                       decoration-danger="state in ('cancel', 'reject', 'expire')"
                       decoration-warning="state == 'draft'"/>
                <field name="sale_order_id" column_invisible="1"/>
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_order_inquiry">
        <field name="name">Order Inquiry</field>
        <field name="res_model">order.inq</field>
        <field name="view_mode">tree,form</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_id" ref="view_order_inq_tree_view"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Order Inquiry
            </p>
        </field>
    </record>

    <record id="saleorder_form_inherit" model="ir.ui.view">
        <field name="name">Sale order</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[1]" position="inside">
                <group invisible="not order_inquiry_id">
                    <field name="order_inquiry_id" readonly="1"/>
                    <field name="inquiry_type" readonly="1"/>
                </group>
            </xpath>
        </field>
    </record>


    <record id="order_inq_reject_wizard_form" model="ir.ui.view">
        <field name="name">order.inq.reject.wizard.form</field>
        <field name="model">order.inq.reject.reason.wizard</field>
        <field name="arch" type="xml">
            <form string="Reject Inquiry">
                <group>
                    <field name="inquiry_id" invisible="1"/>
                    <field name="reason" placeholder="Please enter the reason for rejection" required="1"/>
                </group>
                <footer>
                    <button string="Confirm Reject" type="object" name="action_confirm_reject" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="order_inq_rule_hide_pending_for_wo_user" model="ir.rule">
        <field name="name">Order Inquiry: Hide Pending for WO User</field>
        <field name="model_id" ref="model_order_inq"/>
        <field name="groups" eval="[(4, ref('pr_work_order.custom_group_work_order_user'))]"/>
        <field name="domain_force">[('state', '!=', 'pending')]</field>
    </record>


    <menuitem id="menu_order_inq"
              name="Sale Inquiries"
              parent="sale.sale_menu_root">
        <menuitem id="menu_action_order_inquiry" name="Inquiries" action="action_order_inquiry"/>
    </menuitem>


</odoo>